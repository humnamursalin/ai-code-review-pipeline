{
  "name": "ai-code-review-pipeline",
  "version": "1.0.0",
  "description": "CI/CD pipeline with E2E tests",
  "scripts": {
    "cypress:run": "cypress run",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}

